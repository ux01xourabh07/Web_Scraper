# Web_Scraper

This repository contains web scraping utilities and projects. 

## Projects included:
- `airfare_scraper`: A tool to scrape airfare data.

## Setup
Make sure you have Python installed. You can install the required packages for individual scrapers by navigating into their directories and running:

```bash
pip install -r requirements.txt
```
